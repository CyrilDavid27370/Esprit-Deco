{% extends 'base.html.twig' %}

{% block title %}Espace Administrateur - EspritDéco{% endblock %}

{% block body %}
    <div class="row justify-content-center ">
        <div class="col-12 col-md-8 col-lg-6">

            <h2 class="text-center mb-2">Espace administrateur</h2>
            <p class="text-center text-muted mb-4">Choisissez une section pour gérer une des entités ci-dessous.</p>

            <div class="d-grid gap-3">
                <a href="{{ path('app_admin_product_list') }}" class="btn btn-primary fw-bold">
                    Produit
                </a>
                <a href="{{ path('app_admin_category_list') }}" class="btn btn-success fw-bold">
                    Catégorie
                </a>
                <a href="{{ path('app_admin_user_list') }}" class="btn btn-info fw-bold">
                    Utilisateur
                </a>
            </div>

        </div>
    </div>
{% endblock %}
